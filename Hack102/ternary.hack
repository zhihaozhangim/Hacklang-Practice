<<__EntryPoint>>
function main(): void {
    // echo (false ? "Hello" : "world");
    // ?: - return the first value if the first one is true; Otherwise, return the second one.
    echo false ?: "false";
}